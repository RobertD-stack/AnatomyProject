# Example scripts

This folder contains example scripts that are used by scenes in the
`com.mechdyne.getreal3d/Examples` folder.

Since these scripts are just examples, they shouldn't be used directly. Create
copies in your project and modify them for your use instead.
